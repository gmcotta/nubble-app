import { ViewStyle } from 'react-native';

export const shadowProps: ViewStyle = {
  elevation: 20,
  shadowOffset: { width: 0, height: -3 },
  shadowColor: '#000',
  shadowOpacity: 0.05,
  shadowRadius: 12
};
