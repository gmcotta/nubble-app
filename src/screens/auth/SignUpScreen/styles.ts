import { TextInputProps, TextProps } from '@components';

export const titleStyles: TextProps = {
  preset: 'headingLarge',
  marginBottom: 's32'
};

export const usernameInputStyles: TextInputProps['boxProps'] = {
  marginBottom: 's20'
};

export const fullNameInputStyles: TextInputProps['boxProps'] = {
  marginBottom: 's20'
};

export const emailInputStyles: TextInputProps['boxProps'] = {
  marginBottom: 's20'
};

export const passwordInputStyles: TextInputProps['boxProps'] = {
  marginBottom: 's48'
};
